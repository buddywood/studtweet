<h1>Hello</h1>
<div ng-controller="StudCtrl">
	<form ng-submit="addConference()">
	<input type="text" ng-model="newConference.name">
	<input type="submit" value="Add">
	</form>
	<ul>
		<li ng-repeat="conference in conferences">
			{{conference.name}}
            {{conference.totalMembers}}
		</li>
	</ul>
</div>
{{newEntry.name}} 